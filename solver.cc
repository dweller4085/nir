#include "solver.hh"

Solver::Solver(char const * dimacs, Settings const & settings):
    settings {settings},
    cdb {},
    searchState {}
{
    // parse the dimacs string into the cdb...
}

Solver::Result Solver::solve() {
    
}
